# 2026-05-02

## MkReader 项目启动
- 用户需求：安卓手机上运行的 Markdown 阅读编辑应用
- 技术选型：React + Vite + TypeScript + Tailwind CSS + Capacitor（延续 fanfic-system 技术栈）
- 完成了对 `D:\fanfic-system\src-ui/` 的深度架构审查
- 发现了原方案的 10 个架构问题并已修正：
  1. CSS 变量主题系统 vs Tailwind dark 模式
  2. 缺失安全区域适配
  3. @capacitor/intent 插件不存在
  4. Intent 处理架构需自定义插件
  5. 缺失 app-height 动态计算
  6. 缺失 min-h-[44px] 触控目标
  7. 边框颜色不一致
  8. 缺失 cn() 工具函数
  9. 缺失 input font-size: 16px 修复
  10. Button 组件需要 Tone/Fill 模式

## 方案审查结论
- 计划文件更新于 `C:\Users\Administrator\.workbuddy\plans\blazing-beacon-babbage.md`
- 所有 UI 样式严格延续 fanfic-system 的设计体系
- 用户批准方案，开始执行

## Phase 1-7 完成
- 手动搭建 Vite + React + TypeScript 项目结构
- 依赖安装（@capacitor/preferences 用 v7，v8 只有 nightly）
- CSS 变量主题系统 + 安全区 + app-height — 延续 fanfic-system
- UI 组件：Button (Tone/Fill), Input, Spinner, EmptyState, ThemeToggle
- Markdown：react-markdown + 三种模式 (edit/preview/read)
- Hooks：useMediaQuery, useTheme, useFileSystem, useRecentFiles
- 页面：FileList (底部导航+文件浏览+最近文件+设置), Editor (模式切换+保存)
- App.tsx：状态驱动路由 + visualViewport 监听 + 文件 Intent 接收
- Android：Capacitor 平台已初始化，FileReceiverPlugin.java 已创建
- AndroidManifest.xml 已配置 .md/.markdown/.txt 文件关联
- 前端构建成功：dist ~400KB (vendor-markdown 独立 chunk)
- Capacitor sync 成功

## Phase 8 完成
- useFileSystem 增强：大文件检测（>500KB 显示加载进度）、mkdir 支持
- markdownComponents 增强：img 相对路径解析 + 加载失败降级、a 外部链接、table/th/td 渲染
- .gitignore 已创建
- public/manifest.json 已创建
- 最终构建验证：dist ~396KB，Capacitor sync 成功
- **全部 8 个 Phase 已完成**

## Bug 审查与修复（7 处）
1. `useRecentFiles.addRecent`: Preferences.set 从 setState 回调内移出（React 状态更新函数应为纯函数）
2. `Editor.tsx`: 添加 readError 错误状态，文件读取失败时显示错误信息而非空白页
3. `Editor.tsx`: 移除未使用的 Share2 导入
4. `FileList.tsx`: 移除未使用的 Plus 和 RecentFile 导入
5. `useFileSystem.ts`: 移除未使用的 progress 状态，简化为 loading + 大文件警告日志
6. `App.tsx`: 重排 useCallback/useEffect 顺序，handleFileSelect 定义前置
7. 修复后构建验证通过，Capacitor sync 成功
